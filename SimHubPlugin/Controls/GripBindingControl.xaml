<UserControl x:Class="DiyFfb.Controls.GripBindingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignWidth="780" d:DesignHeight="300">
    <GroupBox Header="Grip Button Bindings" Margin="4">
        <StackPanel>
            <ItemsControl x:Name="BindingList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="2,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="30"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Label}" VerticalAlignment="Center" FontWeight="SemiBold"/>
                            <TextBlock Grid.Column="1" Text="{Binding DisplayName}" VerticalAlignment="Center" Foreground="{Binding DisplayColor}"/>
                            <Button Grid.Column="2" Content="{Binding BindButtonText}" Click="OnBindClick" Tag="{Binding SignalName}" Padding="6,2"/>
                            <Button Grid.Column="3" Content="X" Click="OnClearClick" Tag="{Binding SignalName}" Padding="4,2"
                                    Visibility="{Binding ClearVisibility}"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Button Content="Clear All Bindings" HorizontalAlignment="Right" Margin="4,8,4,4" Padding="10,4" Click="OnClearAllClick"/>
        </StackPanel>
    </GroupBox>
</UserControl>
